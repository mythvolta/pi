# Installation instructions for wiringPi etc.

$ cd ~/code
$ git clone git://git.drogon.net/wiringPi
$ cd wiringPi/
$ ./build

# For arduino loading via the command-line
$ sudo apt install gcc-avr avr-libc avrdude arduino

# For RGB matrix
$ cd ~/code
$ git clone https://github.com/hzeller/rpi-rgb-led-matrix.git
